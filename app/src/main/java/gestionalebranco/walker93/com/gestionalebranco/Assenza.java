package gestionalebranco.walker93.com.gestionalebranco;

/**
 * Created by Workstation 2 on 10/10/2016.
 */

public class Assenza {



/*    public static String ListProveToIDString(List<Specialità> list) {
        String result = "";
        for (Specialità s : list) {
            result = result + .indexOf(s) + ";";
        }
        return result;
    }

    public static List<Specialità> IDStringToProveList(String ids) {
        List<Specialità> result = new ArrayList<>();
        if (ids.length()>=1) {
            String[] split = ids.split(";");
            for (int i = 0; i < split.length; i++) {
                result.add(.get(Integer.parseInt(split[i])));
            }
        }
        return result;
    }*/
}
